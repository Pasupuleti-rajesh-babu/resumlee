OPENAI_API_KEY=sk-...
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Access code gate — set any secret value here to require it on every request.
# Share this code only with people you want to have access.
# Leave empty or unset to disable the gate (useful for local dev).
ACCESS_CODE=
