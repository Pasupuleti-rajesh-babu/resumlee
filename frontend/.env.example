# Local development — points to FastAPI running on port 8000
NEXT_PUBLIC_API_URL=http://localhost:8000

# Vercel deployment with experimentalServices — backend is at /_/backend
# (leave NEXT_PUBLIC_API_URL unset or set it to /_/backend)
# NEXT_PUBLIC_API_URL=/_/backend
